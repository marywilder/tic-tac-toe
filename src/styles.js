
const height = '100vh';
const flex = 'flex';
const center = 'center';

const font = 'Lilita One, cursive';

export const style = {
    
    height: height,
    display: flex,
    justifyContent: center,
    alignItems: center,
    fontFamily: font,
}

